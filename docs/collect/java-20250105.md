## synchronized和ThreadLocal实现的角度和思路是不同的

synchronized是以`时间换空间`，让不同的线程排队访问。而ThreadLocal是以`空间换时间`，为每个线程都设置了一份变量。

`初始`

```java
class ThreadLocalTest {
    private String msg;

    public String getMsg() {
        return msg;
    }

    public void setMsg(String msg) {
        this.msg = msg;
    }

    public static void main(String[] args) {
        ThreadLocalTest test=new ThreadLocalTest();
        for (int i = 0; i < 10; i++) {
            new Thread(()->{
                //先给对象赋值，再取出这个值
                test.setMsg(Thread.currentThread().getName()+"的数据");
                System.out.println(Thread.currentThread().getName()+"---->"+test.getMsg());
            }).start();
        }
    }
}
```

每次输出都不一样
```shell
Thread-2---->Thread-2的数据
Thread-7---->Thread-1的数据
Thread-9---->Thread-1的数据
Thread-5---->Thread-5的数据
Thread-8---->Thread-8的数据
Thread-0---->Thread-0的数据
Thread-1---->Thread-1的数据
Thread-3---->Thread-2的数据
Thread-6---->Thread-1的数据
Thread-4---->Thread-4的数据
```

`使用ThreadLocal后`

```java
class ThreadLocalExample {
    ThreadLocal<String> threadLocal = new ThreadLocal<>();
    private String msg;

    public void setMsg(String msg) {
        threadLocal.set(msg);
    }

    public String getMsg() {
        return threadLocal.get();
    }

    public static void main(String[] args) {
        ThreadLocalExample threadLocalExample = new ThreadLocalExample();
        for (int i=0; i<10; i++) {
            new Thread(() -> {
                threadLocalExample.setMsg(Thread.currentThread().getName() + "的数据");
                System.out.println(Thread.currentThread().getName() + "--->" + threadLocalExample.getMsg());
            }).start();
        }
    }
}
```

```shell
Thread-7--->Thread-7的数据
Thread-2--->Thread-2的数据
Thread-3--->Thread-3的数据
Thread-5--->Thread-5的数据
Thread-9--->Thread-9的数据
Thread-8--->Thread-8的数据
Thread-4--->Thread-4的数据
Thread-6--->Thread-6的数据
Thread-0--->Thread-0的数据
Thread-1--->Thread-1的数据
```

`使用synchronized后`

```java
class SynchronizedExample {
    private String msg;

    public void setMsg(String msg) {
        this.msg = msg;
    }

    public String getMsg() {
        return msg;
    }

    public static void main(String[] args) {
        SynchronizedExample synchronizedExample = new SynchronizedExample();

        for (int i=0; i<10; i++) {
            new Thread(() -> {
                synchronized (synchronizedExample) {
                    synchronizedExample.setMsg(Thread.currentThread().getName() + "的数据");
                    System.out.println(Thread.currentThread().getName() + "--->" + synchronizedExample.getMsg());
                }
            }).start();
        }
    }
}

```


```shell
Thread-0--->Thread-0的数据
Thread-9--->Thread-9的数据
Thread-8--->Thread-8的数据
Thread-7--->Thread-7的数据
Thread-6--->Thread-6的数据
Thread-5--->Thread-5的数据
Thread-4--->Thread-4的数据
Thread-2--->Thread-2的数据
Thread-3--->Thread-3的数据
Thread-1--->Thread-1的数据
```

使用InheritableThreadLocal可以实现多个线程访问ThreadLocal的值。

```java
// Thread-0获取了null
public static void main(String[] args) {
    ThreadLocal threadLocal=new InheritableThreadLocal();
    threadLocal.set("主线程中的数据");
    new Thread(()->{
        System.out.println(Thread.currentThread().getName()+"获取了"+threadLocal.get());
    }).start();
}

// Thread-0获取了主线程中的数据
public static void main(String[] args) {
    ThreadLocal threadLocal=new InheritableThreadLocal();
    threadLocal.set("主线程中的数据");
    new Thread(()->{
        System.out.println(Thread.currentThread().getName()+"获取了"+threadLocal.get());
    }).start();
}


```

## java 定时任务 xxl-job

【含完整代码】Java定时任务之xxl-job[超详细]_java xxl-job-CSDN博客
https://blog.csdn.net/wang20000102/article/details/135575904

## java 定时任务（5） 线程类实现定时任务+Timer/TimerTask+ScheduledExecutorService+@Scheduled注解+Quartz 框架

线程类实现定时任务+Timer/TimerTask+ScheduledExecutorService+@Scheduled注解
https://www.jb51.net/program/332785rbe.htm


Quartz 框架
